apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: horizon-bot

labels:
- pairs:
    app: horizon-bot-homepage

resources:
  - deployment.yaml
  - service.yaml
  - ingress.yaml
  - image-updater.yml
  - pdb.yaml
